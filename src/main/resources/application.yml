spring:
  datasource:
    url: jdbc:mariadb://13.124.207.219:3306/lgi_test
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: 2022
#    initialization-mode: always
#   sql-script-encoding: UTF-8
  security:
    user:
      name: fooding
      password: fooding
      role: USER
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: false
    defer-datasource-initialization: true
  logging:
    level:
      org.hibernate.SQL: debug
      org.hibernate.type: trace
  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false